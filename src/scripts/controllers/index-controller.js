





var IndexController = function($scope) {
	
}

// Register the controller.
angular.module('braunianApp').controller('IndexController', IndexController);

// Inject the relevant factories and services.
IndexController.$inject = ['$scope'];