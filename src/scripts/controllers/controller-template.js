/**
 *	Controller Template.
 *	 
 *	[Controller description]
 */
var Controller = function($scope, service_1, service_2) {
	
	
	
};

// Register the controller.
angular.module('braunianApp').controller('Controller', Controller);

// Inject the relevant factories and services.
Controller.$inject = ['$scope', 'service_1', 'service_2'];